source 'https://rubygems.org'

gem "sinatra"
gem "sass"
gem "activerecord"
gem "sinatra-activerecord"
gem 'sinatra-flash'
gem 'sinatra-redirect-with-flash'

group :development do
 gem 'sqlite3'
 gem "tux"
end

group :production do
 gem 'pg'
end

# To deploy on Heroku, uncommenting these lines should be enough to get everything working fine
# group :production do
#   gem 'therubyracer-heroku', '0.8.1.pre3'
#   gem 'execjs'
# end
